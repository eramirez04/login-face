// Tipos de eventos
export const SOCKET_EVENTS = {
    CONNECT: 'connect',
    DISCONNECT: 'disconnect',
    SUSCRIBIRTOPRODUCTO: "subscribeToProduct"
};


// Socket State Interface
export interface SocketState {

}



const socketReducer = (state, type: ) => {
    switch (){
        case SOCKET_EVENTS.CONNECT:
            return {
                ...
            }
    }
}